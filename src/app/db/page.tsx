import type { ReactElement } from "react";

import { DbItemClassHubPage } from "@/components/pages";

const DbPage = async (): Promise<ReactElement> => {
  return <DbItemClassHubPage />;
};

export default DbPage;
