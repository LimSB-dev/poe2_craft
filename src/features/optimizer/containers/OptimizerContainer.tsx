"use client";

import type { ReactElement } from "react";

import { OptimizerView } from "@/components/organisms/OptimizerView";

export const OptimizerContainer = (): ReactElement => {
  return <OptimizerView />;
};
